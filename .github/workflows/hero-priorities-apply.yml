name: Validate hero priorities and regenerate pages
# Retired by owner-approved reconciliation. Never reapply the old source payload.
on:
  workflow_dispatch:
permissions: {}
jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired; use the authoritative integration branch.'
