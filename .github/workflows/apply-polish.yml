name: Apply and validate systematic polish release
# Retired by owner-approved reconciliation. Original preserved in Git history and backup.
on:
  workflow_dispatch:
permissions: {}
jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired; use the authoritative integration branch.'
