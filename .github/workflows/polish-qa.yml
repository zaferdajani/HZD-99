name: CLAWBYTE polish release snapshot
# Retired after the complete all-ref history backup.
on:
  workflow_dispatch:
permissions: {}
jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired; preserved in reconciliation backup.'
