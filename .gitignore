node_modules/
www/
tests/out/
android/.gradle/
android/app/build/
android/build/
android/local.properties
android/app/src/main/assets/public/
ios/App/App/public/
ios/App/Pods/
.DS_Store
.claude/worktrees/
.claude/agents-run/
build/
reference/movement/

# the art session's working folder: 74 MB of intermediate plates and contact
# sheets. Anything worth keeping is derived into assets/source/ as jpg first.
scratchpad/

# The PCN platform and the investor materials live in the PRIVATE repo
# zaferdajani/app-mall (branches `main` and `business-archive`). They were once
# on a branch of THIS repo, which is public, and were moved out deliberately.
# Ignored here so a stray working copy can never be committed back into a public
# repo by a well-meaning "commit your changes" prompt.
platform/
deck/
# the cast reel's working folder (tools/castreel.cjs): 26k frames and a
# 160 MB film, regenerated from the game rather than kept — ledger §2at
restyle/
